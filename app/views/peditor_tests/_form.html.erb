<%= form_for(@peditor_test) do |f| %>
  <% if @peditor_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@peditor_test.errors.count, "error") %> prohibited this peditor_test from being saved:</h2>

      <ul>
      <% @peditor_test.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
